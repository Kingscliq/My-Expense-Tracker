/** @format */

import React from "react";

const Header = ({ title }) => {
  return (
    <h2
      style={{ color: " #332c55", textAlign: "center", marginBottom: "40px" }}
    >
      {title}
    </h2>
  );
};

export default Header;
